<%= form_for(@compiler) do |f| %>
  <% if @compiler.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@compiler.errors.count, "error") %> prohibited this compiler from being saved:</h2>

      <ul>
      <% @compiler.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :command %><br>
    <%= f.text_area :command %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
